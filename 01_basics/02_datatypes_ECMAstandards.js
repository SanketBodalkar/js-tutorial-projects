//ECMA standards are the standards that define the core features of JavaScript. The ECMA-262 standard defines the ECMAScript language,
//  which is the basis for JavaScript. The ECMA-402 standard defines the Internationalization API, 
// which provides support for internationalization and localization in JavaScript.
// it defines the coding conventions, syntax, and semantics of the language,
//  as well as the built-in objects and methods that are available in JavaScript.

"use strict"; //use strict will treat all js code as modern js code and will throw error for any deprecated or old js code

// alert(3 + 3); // this will throw an error because we are using node js not browser environment.

// code readability and maintainability is important in modern JavaScript development.

//Datatypes in JavaScript
// JavaScript has several built-in data types, which can be divided into two categories: primitive and non-primitive (or reference) data types. 

//number
//bigint
//string
//boolean
//undefined
//null
//symbol

//object

// to fetch the type of a variable, we can use the typeof operator. 
// The typeof operator returns a string that indicates the type of the operand.